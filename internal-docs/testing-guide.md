# Testing Guide

The maintained reference for Isomux's test suite: the tiers, how to run them, the seams, and the conventions. This is the living companion to [`generic-runtime-refactor.md`](generic-runtime-refactor.md) (the server-refactor design and decision record): that doc explains *why* the test net was built this way, this one tracks *what exists* and how to work with it. Keep it current as the suite evolves.

## How to run

- `bun test` - the default. Zero LLM calls, so it is safe for CI, PRs, and pre-commit. This is the whole suite except the live smoke tier.
- `bun run test:live` - `ISOMUX_TEST_LIVE=1 bun test`. Adds the gated live smoke tier (T3 below): a few real-subscription, end-to-end checks against real backends. Run it manually or nightly, never in CI or pre-commit.
- `bun run test:systemd` - `ISOMUX_TEST_SYSTEMD=1 bun test server/app-supervisor.live.test.ts`. The one test that drives real systemd: installs a unit, starts an app, kills it, reads the restart count, and removes everything it made. Its own gate rather than `test:live`'s, because that flag means "spend model credits" and this test spends none - conflating them would make one half cost money and the other half create unit files.
- `bun run lint` (ESLint) and `bun run format:check` (Prettier) round out the pre-review checks.

## Tiers (LLM-call policy)

Default `bun test` makes zero LLM calls.

| Tier | LLM? | What | When |
|---|---|---|---|
| T0 pure/unit | none | pure functions: prompt assembly, diff summary, fork-chain assembly, usage math, projection helpers | always |
| T1 integration (FakeBackend + non-backend harnesses) | none | the main tier: orchestrator/queue/fork/persistence/OfficeState, plus auth/projection/presence/route harnesses; real temp FS, in-process server | always |
| T2 adapter contract | none (replay) | real Claude/Codex adapters translate recorded/curated event streams correctly | always; refresh on SDK bumps |
| T3 live smoke | yes, a few | real-subscription end-to-end; assert invariants only (turn completes, a tool runs, resume works, topic non-empty and at most 8 words), never exact text | opt-in only (`test:live`) |

T3 is gated behind the `ISOMUX_TEST_LIVE` env flag plus the separate `test:live` script, run manually or nightly, serial, on cheap models. Never in CI or pre-commit.

## Principles

1. Test behavior through public interfaces, not implementation. Tests assert observable behavior at public boundaries (WS messages, REST responses, persisted files), never private internals, so they survive refactors.
2. Two modes, two jobs. Characterization tests freeze existing externally visible behavior (the refactor safety net); TDD red-green-refactor drives the new code (projection service, REST contract, token auth, stable room IDs).
3. The feature inventory as a coverage map, not a mandate. Every feature in the canonical inventory (`docs/features.md`) has at least one behavioral test of its critical mechanism at the lowest deterministic tier. Tracked via the traceability matrix below.
4. Avoid horizontal slicing. Do not write all tests up front against speculation: characterization tests are grounded by observation, new-code tests follow red-green-refactor.

## Seams and where they live

`FakeBackend` (implements the `Backend` interface, scripts `NormalizedEvent`s) is the main deterministic engine for T1: the Backend seam is clean and metaphor-free, and the projection/stable-id work sits above it. It is injected through constructor DI (`ManagerDeps` into `AgentManager`, `CronjobManagerDeps` into `CronjobManager`). Tests: `fake-backend.test.ts`, `agent-manager.di.test.ts`, `cronjob-manager.di.test.ts`.

Surfaces that are not below the Backend seam have their own harnesses. Tests under `server/test-support/` are named bare; tests elsewhere carry their path.

- **Auth / session / invite / token** - cookie and token auth, owner/member roles, session revocation, the removed loopback bypass (agent affordances, then the `/tasks` / `/cronjobs` / `/backup/status` prefixes), token lifecycle (mint/rotate on spawn/revive, revoke on kill/delete), redaction (the token never appears in prompts, logs, errors, diffs, or over WS), and the object-level guards (`agentParamMustEqualTokenAgent`, `requiresRoomAccess`, `cronjobOwnerOrOfficeOwner`, `selfOrOwner`). Tests: `routes-auth.test.ts`, `identity-tokens.test.ts`, `identity-lifecycle.test.ts`, `guards.test.ts`, `guard-combinators.test.ts`, `guard-deps.test.ts`, `routes-invites-rest.test.ts`, `routes-sessions-rest.test.ts`, `routes-access-rest.test.ts`.
  The four files above cover auth as reached through the REST/WS routes. `server/auth.ts` itself - the module the routes delegate to - is covered directly by four more, added once the module's invariants outgrew what route tests happened to touch:
  - `auth-invites.test.ts` - the invite half. The TTL precedence ladder (`ttlMsOverride` > the 1h self-invite window > the standard 24h `INVITE_TTL_MS`; no client wire carries the override, so the ladder is the only thing keeping a self-invite from a 24x wider bearer-URL exposure), `replacePriorForUsername`'s exact blast radius (same username case-insensitively, outstanding rows only - consumed, expired and foreign rows all survive), `peekInvite` never consuming (link unfurlers and prefetch must not burn a one-time token), the mint and accept refusal matrices including the two that exist only for between-mint-and-accept races (`role_mismatch`, `owner_exists`), concurrent acceptance of one token resolving to exactly one winner through the module mutex, and the bootstrap sweep that retires sibling invites the moment an owner exists.
  - `auth-sessions.test.ts` - the session half. Both expiry clocks invalidating independently AND evicting the row (a session that reads dead but lingers would still be counted by the lockout arithmetic), the rolling refresh clamped to the absolute cap, orphan sweep when a user record vanishes, revoke/logout/evict force-closing registered sockets with `session_expired` sent *before* the close, and the lockout arithmetic reading role from the user RECORD so promotions move the last-owner line. Two security-critical shapes get their own describes: **prefix-collision refusal** (planted on disk and cold-reloaded, since 8-char prefixes of a 256-bit token can't be forced) and **lockout-check ordering** - the scoped session revoker must run "would this strand the office" only *after* confirming the row belongs to the caller, or the refusal becomes an oracle telling any member which prefix is the last owner's.
  - `auth-wall.test.ts` - the transport gate through the real server. The WS upgrade matrix, including the deliberate asymmetry that a *missing* Origin is refused on `/ws` but allowed on a mutating HTTP POST (agent curl sends none; a browser always does); expired and revoked cookies refused identically to absent ones; the PWA pre-auth whitelist asserted in BOTH directions (`/manifest.json` and `/icons/*` open, app shell and bundles still walled - a one-sided test passes just as well if the whole wall came down); the two-step invite flow over HTTP; and a rename leaving in-flight sessions intact.
  - `auth-session-migration.test.ts` - the `sessions.json` `username` → `userId` migration over a REAL cold reload (`restart()`), the half `migrations.test.ts` never covered: legacy rows resolving case-insensitively via `getUserByName`, orphans evicted rather than pointed at a dangling id, undated legacy rows failing closed, and the write-back sticking so the migration is a one-time event.

  Two test-only seams in `server/auth.ts` support these: `_testSetSessionExpiry` (back-date a session's stamps instead of mocking the clock or waiting 30 days) and `_testResetState`, which drops both maps to `null` - the not-yet-loaded sentinel - so a harness `restart()` genuinely re-reads `invites.json` / `sessions.json` the way a real process restart does. (It previously assigned empty Maps, leaving the module permanently "loaded" with nothing, so a cold restart silently served empty auth state and rejected every still-valid cookie. That is why the on-disk migration and prefix-collision cases were untestable before.)
- **Projection / fanout** - multi-WebSocket tests with different users and access grants; the event registry (every event type declares an audience and projection, a single emit helper is the only wire path, and no raw `ws.send`/`broadcast` exists outside the dispatcher); preference writes never leak hidden-room existence. Tests: `projection.test.ts`, `emit.test.ts`, `event-registry.test.ts`, `dispatch.test.ts`, `view-routes.test.ts`, `user-settings.test.ts`, `user-wire.test.ts`.
- **Presence** - its own `connectionId` map and recipient-specific room remapping. Tests: `presence.test.ts`.
- **Task board delta push** - a task mutation sends the ONE task that moved, not the whole board, and what a recipient is told (`task_upserted` / `task_deleted` / nothing) depends on their room access BEFORE and AFTER the change. The truth table is pure and lives in `server/events/task-delta.test.ts`; the wire behavior, including a re-file out of a member's rooms arriving as a delete and never leaking an id they could not see, is in `routes-tasks-rest.test.ts`; the reducer's replace-or-append arms are in `ui/store.test.ts`.
- **Route contract** - the typed route table, the two-stage dispatcher, idempotency, and the per-resource REST handlers. Tests: `routes-table.test.ts`, `route-executor.test.ts`, `route-match.test.ts`, `idempotency.test.ts`, and the per-resource `routes-*-rest.test.ts` family (agents, conversation, editor, rooms, users, cronjobs + runs, office-settings, backends, uploads, validate, system, tasks, access, invites, sessions, affordances).
- **Control-plane provisioning** (`control-plane/`, T0/T1, no box required) - the provider adapter against recorded Contabo responses through an injected transport, the SSH driver against a fake process seam, and `wrapper.sh` against a fake installer in a temp tree. Tests: `contabo/adapter.test.ts` (find never claiming `exact` when the server ignored the filter, returned several matches, or paginated; create's ambiguous-vs-rejected classes), `intents.test.ts` (the pre-call create latch surviving simulated process loss at the network boundary), `ssh.test.ts` (the flags the revocation proof depends on, the authentication classifier, and the shell-inert guard on remote arguments), `driver.test.ts` (the expiry precondition and read-back, launch/tick parsing, the removal proof, host-key pinning), `wrapper.test.ts` (generation isolation, exit capture, single-flight and crash detection, driven through real bash/flock/setsid), `redaction.test.ts` (no key material or invite URL reaching audit JSONL, stdout, stderr or the transcript), `store.test.ts` (version CAS, lease adoption, the one-active-operation index, deadline flagging that writes no status, and attention reasons that cannot overwrite each other), `create-latch.test.ts` (the schema latch: one transaction arming intent plus operation, the single-use permit, the legacy journal as a veto, and a REAL child process killed inside the provider call to prove a restart can never reach `adapter.create`), `tick.test.ts` (fencing, the chain's one-transaction completion, remote timeouts as ambiguous by default, deadlines that flag without concluding), `handlers.test.ts` (the installer's persisted-runId-before-launch ordering, the known_hosts remove-then-record ordering, and invite URLs reaching nothing durable), `roles.test.ts` (the connection posture as arithmetic - the two engine-enforced DEPLOYED budgets summing under the measured ceiling, a redeploy overlap fitting inside the provisioner's, and no statement attempting to cap the owner, which this provider refuses - and the privilege matrix by its ABSENCES: no DELETE anywhere, no create latch for the web tier, no `update` on operations for it, and no default privileges), `governance-apply.test.ts` (the posture against a REAL database: a fresh empty one reaching schema-ready with roles governed and granted in the one order that works, a failing statement leaving no partial catalog change, an excess table privilege and an excess role setting both erased by a rerun, and an owner carrying unknown configuration refusing before it mutates anything), `store-governance.test.ts` (where a session's governed bounds may come from: a managed session takes them from the role or refuses, and a role whose configuration was reverted refuses EVEN WHEN the connection string still asks for both bounds the old way - a local container keeps the mechanism it always had), `deploy/tree-state.test.ts` (the deploy guard classifying uncommitted work against what HEAD carries rather than against a porcelain prefix, so an intent-to-add freeze is not read as 27 dirty runtime paths).
  The setup-access policy has its own mutation-strength seam: `access-window-policy.test.ts` pins the seven-day maximum and operator clamp; `signup.test.ts` pins the immutable signup snapshot, moved-clock retry and pre-change-row refusal; `instance.test.ts` rejects new and existing longer windows; `handlers.test.ts` refuses an out-of-policy row before first contact touches the box; and `driver.test.ts` proves the persistent cleanup timer carries the stored instant exactly. The on-box `expiry-test` command separately proves a scratch key authenticates before its deadline and that sshd rejects it after, including across a power-off. Retained-office re-subscription is pinned in `reinstatement.test.ts`: the strict boundary, same-account and customer-access predicates, session generations, fetched Checkout expiry shapes, both linkage/deletion orderings, payment-status evidence, and cancel -> reinstate -> cancel. `web/e2e/lifecycle.e2e.ts` carries the same pending boundary through the real projection, Next route, and browser.
- **Control-plane billing** (`control-plane/stripe/`, T0/T1, no Stripe account required) - the Stripe transport, webhook handling and dunning ladder against an injected object reader and synthesised snapshots, so every branch runs without a live account. Tests: `signature.test.ts` (the signed material being `timestamp.body`, the window rejected in BOTH directions, several v1 candidates, constant-time compare), `client.test.ts` (the live-key refusal before any request is issued, bracketed form encoding, an ambiguous write repeated with the SAME idempotency key), `shapes.test.ts` (the fields the pinned API version moved: period end on the subscription ITEM, an invoice's subscription under `parent.subscription_details`, a discount reached through `source.coupon`, and an invoice with no `paid` boolean), `billing-store.test.ts` (the two setters with disjoint columns, the event-id primary key as the dedupe, and each guarded billing column refusing an older database on its own), `dunning.test.ts` (the ladder as a pure function: exhaustion, the couponed diversion, hold expiry that never suspends on the calendar alone), `webhook.test.ts` (the live-mode gate touching nothing, a throw mid-apply leaving the event replayable, two same-second events unable to regress state, and one suspension per dunning episode across a replay, a second exhaustion event and a state regression), `billing-tick.test.ts` (both hold-expiry branches), `checkout.test.ts` (`if_required` only on the comped path, and Checkout writing nothing to the store), `suspension.test.ts` (the power_off handler against a stubbed provider, and what happens when no handler is registered), `fixtures.test.ts` (no credential or personal data in fixtures, a synthetic live-mode delivery refused, and recorded deliveries still parsing), `ownership.test.ts` (a source scan: only reconciliation may write the cache of Stripe truth), `cleanup.test.ts` (ownership proven positively before anything is deleted from the shared test account, and an incomplete list reported rather than treated as the whole account).
  The wrapper and redaction files are worth knowing about: the wrapper's invariants (a lock fd the installer cannot inherit, an exit trap that reads `$?` when it fires, a tick that re-reads the exit file before calling anything a crash) cannot be made to fail on demand against a live box, so they are proven locally instead. `ISOMUX_CP_ROOT` exists only so those tests can point the protocol at a temp tree, the same seam `deploy/install.sh` uses; the driver never sets it.
- **Stable room IDs** - `roomId` as the authority across server-internal logic and the wire. Tests: `roomid-authority.test.ts` (plus the room-id assertions threaded through `projection.test.ts`).
- **Persistence / migration** - round-trip write/load for agents/users/tasks/cronjobs, the load-time migrations, the stable-room-IDs flatten, and the configurable state root. Tests: `persistence.test.ts`, `migrations.test.ts`, `access-migration.test.ts`, `temp-state.test.ts`, `server/config.test.ts`.
- **Queue / fork / usage** - the message queue and coalescing, conversation branching, and usage accounting, plus the `/isomux-usage` report's room scoping (a member sees only their rooms; cron jobs are owner-only). Tests: `queue.test.ts`, `fork-usage.test.ts`, `usage-scoping.test.ts`. Editing a message that carried attachments (the notice block a backend appends has to be stripped back off before the edit matcher can find it, and the attachments ride onto the edited turn): `edit-attachments.test.ts`, with the strip helper itself in `server/attachment-prompt.test.ts`.
- **Agent failure states** - what the operator sees and can do when a backend dies or an agent parks on a permission prompt: the death-message mapping (exit code 143/137 to a sentence naming the signal, the likely OOM cause and the recovery; the `[ede_diagnostic]` blob replaced; the raw string kept in the log entry's metadata) in `server/backend-failure-text.test.ts`, and the orchestration around it through the harness in `agent-death-recovery.test.ts` - a mid-turn kill modelled as `endStream()` with no `turn_completed`, queued messages surviving a recovery resume and then flushing, the queue still cleared (loudly) on a resume that switches sessions, `send-now` refusing on a dead backend rather than answering 204, and a prompt-parked agent being visible on the roster and the logs API and actually stoppable. Resuming a Claude session needs its `.jsonl` to exist, so the test stubs one under a `CLAUDE_CONFIG_DIR` pointed at the harness temp root - never the real `~/.claude`.
- **Scheduled messages** - the deliver-later manager (`createScheduledMessageManager`, `ScheduledMessageManagerDeps`: enqueue, display lookup, notify, persistence, clock/scheduler seam - the cronjob-manager DI pattern): firing/catch-up/retry/deadline/ordering, the at-least-once crash window, quota, restart-surviving idempotency, and the corrupt-file quarantine, all against fakes (`scheduled-messages.di.test.ts`); the deliverAt REST branch + outbox routes + restart survival + the scheduled flush-prefix rendering through the harness (`routes-scheduled-messages-rest.test.ts`).
- **Backends / adapters** - the Claude and Codex adapters translating recorded/curated streams, auth-error detection, and subprocess lifecycle. Tests: `server/backends/claude.test.ts`, `server/backends/claude.session.test.ts`, `server/backends/claude.v1-adapter.test.ts`, `server/backends/codex/adapter.test.ts`, `server/backends/codex/native-bin.test.ts`, `server/backends/auth-detect.test.ts`, `server/fable-model.test.ts`. Background-task lifecycle breadcrumbs (`TaskBreadcrumbTracker`: background/foreground filtering, dedupe, skip_transcript, label sanitize): `server/backends/task-breadcrumbs.test.ts`. Live smoke (T3): `server/backends/live-smoke.test.ts`.
- **Safety / plugin hooks** - pre/post orchestration and prompt/env assembly, including a mem0-plugin compatibility pin (plugin pre/post hooks fire and their injected content reaches the prompt). Tests: `server/plugin-hooks.test.ts` (which also covers the two built-in outbound notices' formatters, context-fullness and memory size; the memory notice's delivery lifecycle - once per conversation, re-armed by `/clear`, not by a model change, not consumed by a send that straddles a `/clear`, and still fired after a failed edit-fork rolls the same conversation back - is driven end to end in `server/test-support/memory-notice.test.ts`). The PreToolUse guards themselves (destructive commands, process kills, `~/.isomux/` writes, secret reads, and the fail-closed path extraction) are driven through `createSafetyHooks()` in `server/safety-hooks.test.ts` - the write-protection root is `STATE_ROOT`, which the preload has already redirected to a temp dir, and the test re-asserts that before probing any denial.
- **Onboarding / fresh install** - welcome agents spawn across the three backend-availability states. Tests: `onboarding.test.ts`.
- **Release channel** - git-derived version identity against throwaway repos (`server/version.test.ts`), the `/readyz` per-IP limiter with an injected clock (`server/ready-limiter.test.ts`), `GET /api/version` + `/readyz` through the harness (`routes-system-rest.test.ts`), and the release/updater shell scripts driven end-to-end in sandboxes - temp git origins, PATH-stubbed `systemctl`, a stub `bun`, a flag-gated in-process readiness server, temp state roots - including the updater's failure/rollback ladders (`scripts/release-sh.test.ts`, `scripts/update-sh.test.ts`), plus the installer's default-ref resolver extracted via sed and driven with a stubbed curl (`deploy/install-resolve-ref.test.ts`). `install.sh` has no unprivileged execution harness as a whole (preflight requires root), so `deploy/install-sh.test.ts` pins its load-bearing source instead: the polkit/update-unit escalation, and the browser step (Chrome's .deb never snap, a real capture probe as the service user, every failure path a warning rather than a `die`). Individual functions are still extracted with sed and RUN, against PATH stubs and temp roots: the dependency-sync mode's Caddy invariant, on a stateful `systemctl` stub that really holds active/enabled (`deploy/install-deps-mode.test.ts`); `apt_install`, on an `apt-get` stub that behaves like dpkg's conffile handling, so an operator's hand-edited config surviving a package run - and being named in the output - is exercised rather than asserted (`deploy/install-apt-conffiles.test.ts`); the final report's invite handling, both branches, the terminal one through a real pty from `script` so the `[[ -t 1 ]]` decision itself is under test (`deploy/install-report.test.ts`); and the codex-sandbox step, against a `bwrap` stub that refuses to unshare until an `apparmor_parser` stub loads a profile, so the probe gate is exercised rather than pattern-matched - a box where bwrap works is provably untouched, a broken one provably ends up with the two-stage profile and never a weakened policy (`deploy/install-codex-sandbox.test.ts`; its PATH is a symlink farm of `/usr/bin` minus `bwrap`, because the test box has a real one, and it extracts only the FIRST definition of `run`/`write_file`/`as_service_user` - the embedded helper scripts define their own further down the file, and a plain sed range would test the wrong function). The script tests spawn bash asynchronously (`Bun.spawn` + await): a blocking `spawnSync` would freeze the event loop and the in-process readiness server could never answer the script's curl polls.
- **Provisioning hardening** - the installer's root-reachability gate. `deploy/harden-ssh.sh` runs end-to-end against PATH-stubbed `ssh`/`sshd`/`sudo`/`runuser`/`id`/`getent`/`passwd`/`ip`/`systemctl`, on a copy with the root guard stripped (`deploy/harden-ssh.test.ts`; the guard itself is pinned in the same file). The load-bearing assertion is that the verdict follows the ssh AUTH TRANSCRIPT and never the exit status: a successful login that exits 255 is still a FAIL, a refusal that exits 0 is still a PASS, and an unreadable outcome exits 2 - "could not tell" is not a pass. The other false-PASS traps are covered too, each as its own case: getting in on one address family while the other denies (the ssh stub answers per `root@<addr>`), a server banner carrying the string `Permission denied`, a `NOPASSWD` sudo rule that refuses `sudo true`, a sudo query that fails for an unrecognised reason, a broken `runuser`, the probe running without `-F /dev/null` (the stub reports it), a root-accepted key file the login attempt never offered, a key the account's ssh config names outside any `.ssh` directory (via a stubbed `ssh -G`), and a second SSH daemon on another port - that last one against a real `Bun.listen` socket that answers with an SSH banner, since the endpoint sniff opens a real connection. The mirror case, a commented-out line in root's key list, must stay a PASS. `deploy/install-sh.test.ts` covers the installer side by source pin: both gates present and ordered (after `create_service_user`; after `wait_for_server` but BEFORE `claim_owner`, so a failure leaves no owner and no invite), both dying on FAIL *and* on UNKNOWN, no skip-shaped variable in the parameter block, and the embedded copies of `deploy/harden-ssh.sh` / `deploy/oom-protect.sh` byte-equal to the repo files (regenerate with `bun run scripts/embed-deploy-scripts.ts`).
- **Memory pressure** - the two halves of the out-of-memory story, both driven against fake `/proc` trees so no test ever writes a score onto a real process. `server/oom-stamp.test.ts` covers the in-server sweep that raises the office's own descendants above the office (`server/oom-stamp.ts`, started from `runOfficeMain`): the tree walk finds children and grandchildren but never the server itself and never a process that was reparented away, a parent is visited before its children (so a process born mid-sweep inherits a stamp instead of needing one), and the stamp NEVER lowers a score. The refusal cases are the load-bearing ones - a write that throws, a write that is accepted and then not honoured (through the `writeAdj` seam; a real filesystem cannot produce that branch, and it is precisely the failure the readback exists for), and a pid recycled between the snapshot and the write, which must not be written to at all. Silence is also pinned, because the sweep runs every ten seconds for the life of the office: it says what it did once and then goes quiet, and it names a process it could not mark once rather than once per sweep. `deploy/oom-protect.test.ts` covers the root-side tool through its `PROC_ROOT` seam: the readback discipline from task c5b4e89e (a refused write reported loudly, a recycled pid never reported as a success, the identity check bracketing the READBACK and not just the write), finding a user-level office by cgroup rather than by asking systemd, and the `--restamp` mode that the `isomux-oom-restamp` timer runs every minute - which must print nothing at all when the office already carries the right score and re-apply it loudly when the office has restarted. The timer's own units are asserted from a `--dry-run` run: `ExecStart` pointing at `/usr/local/sbin/isomux-oom-protect`, the refresh of that copy that has to go with it (an older one would answer `--restamp` with a usage error every minute), and the `SyslogLevel`/`LogLevelMax` pair that keeps a once-a-minute oneshot out of the journal.
- **Storage retention** - the disk-usage breakdown and the manual pruner, both over injected roots so no test ever stats or unlinks anything under the real `~/.isomux`. `server/storage-usage.test.ts` builds a fixture state root under the OS temp dir (cleanup via the temp-state guard) and pins the category split, the sum-to-total invariant, symlinks counted but never followed, and the memoized read. `server/storage-prune.test.ts` is mostly a survival matrix: the pruner must never propose an active session, an agent's K newest, a session another was forked from (`loadLogWithAncestors` reads ancestors forever, and deleting one truncates the descendant SILENTLY - chains, siblings, and a cyclic sessions map are all pinned), an attachment any surviving transcript still references, or an attachment still owed to an UNDELIVERED queued message (queue attachments reach no transcript until the queue flushes, so the reachability scan is blind to them - the deps inject the live queue unioned with the durable `message-queues.json`, and an UNREADABLE queue file is `null`, not an empty set - unknown fails closed, spares everything under its own `queue-state-unknown` reason, and makes the route refuse an attachment apply with 409 rather than report a silent "deleted 0"). Symlink containment is pinned three ways: a symlinked `files/` or agent dir is never listed, and before each unlink apply requires the parent to resolve under the real logs root AND no parent component to be a symlink (the second catches an intra-logs symlink, where realpath containment passes but the file belongs to an agent whose transcripts were never consulted). `applyPrune` re-plans against live state, and a candidate that escapes the logs root ABORTS the whole run rather than deleting the well-formed remainder. The residual same-user TOCTOU (no `unlinkat` in Bun) is documented in the module header, not claimed as closed. Route auth + the dry-run default: `routes-storage-rest.test.ts`. ROUTE CAPS: `routes-table.test.ts`. The chat-facing view of the same measurement, `/isomux-storage`, renders through a PURE formatter, so `server/storage-report.test.ts` hands it a hand-built `StorageUsage` and never touches a disk: the headline total counting the out-of-root locations on top of the state root, an unconfigured location reading "none" rather than zero, the owner/non-owner split (the `aggregateOnly` projection must carry sizes but no paths and no per-agent rows), and a stated truncation count. Agent names are user-controlled, so a name carrying a pipe, a newline, or a trailing backslash is pinned to stay INSIDE its table cell, and the "(killed)" annotation is renderer-owned (a flag on the label, appended after escaping) so a name cannot forge it. The handler's CHOICE of projection is a separate boundary and gets its own file, `server/test-support/storage-command.test.ts`, which drives the real handler through `createCommandHandling` with the measurement injected via `deps.getStorageUsage`: owner gets paths + agent rows, a signed-in member gets sizes only, and an invocation with no user record is refused WITHOUT measuring at all (the command's stand-in for the route's 403 - a refusal that still walked the disk would hand anyone who can post a message a free ~10k-inode spin).
- **Conversation-log search / retrieval** - the read core over fixture JSONL, plus the child process that isolates the scan. `log-search.test.ts` is written around the one rule that is easy to break by accident: MATCHING RUNS ON DECODED CONTENT. Every "a raw grep would miss this" case (escaped quotes, embedded newlines and tabs, non-ASCII) is asserted positively, and `canPrefilter` is asserted to REFUSE exactly those queries - the prefilter is a speed optimization that is only sound as a SUPERSET test, so its refusal condition is the actual mechanism under test and gets pinned separately from the searches that depend on it. The fork asymmetry is deliberate and pinned both ways: `session=<id>` reconstructs the ancestor timeline (a forked JSONL holds only branch-local entries), while an all-session scan reads each PHYSICAL entry once so a shared ancestor is not reported per branch; `reconstructTimeline` is additionally pinned against `persistence.loadLogWithAncestors` on the same fixture so the duplicated walk cannot drift. Also: the tier ladder, the `around` window INCLUDING an anchor its own tier filters out (dropping it would answer a question about an entry with a window excluding it), tail-not-head truncation, and the `truncated` / `timedOut` split - "more than your limit" and "I stopped looking" are different facts and are never conflated. `log-search-isolation.test.ts` is the ReDoS proof, and it is written around MEASUREMENTS rather than folklore, because three pieces of the folklore turned out to be false in this runtime. (1) A single catastrophic match does NOT run forever: it is bounded near 0.9s and does not grow with input size, so the textbook "one exec() that never returns" never reproduced. What runs away is the AGGREGATE - twenty such matches measured 17.8s of solid CPU. (2) `worker.terminate()` does NOT kill a Worker: termination is only observed when the worker returns to its event loop, so two workers doing identical work behaved completely differently (the one awaiting between matches died 311ms after terminate; the one with no await never died and held a core at 100%). That is why the scan is a child PROCESS and not a Worker. (3) SIGKILL on a process DOES stop it - 3ms even for the non-yielding spinner - which is what makes the endpoint's 504 a true statement rather than a hopeful one. The load-bearing test therefore uses a deliberately NON-YIELDING fixture (enough bait entries to be expensive, few enough to land in one stream chunk, so the scan is one uninterrupted synchronous burst with no interruption point), and asserts three things: the request returns near the deadline, a main-thread heartbeat kept ticking throughout, and - the part that separates a real kill from merely giving up waiting - the admission slot comes back in well under SCAN_BUDGET_MS. That last assertion matters because the child would ALSO exit on its own once its cooperative budget expired, so a test that just waited "long enough" would pass identically with no kill at all; mutation-tested by removing the SIGKILL, which makes it fail at 6.3s. The timing assertions are self-calibrating (one match timed on the machine running the test, multiplied by the fixture size), and the fixture fails loudly if the bait pattern ever becomes cheap so it cannot pass vacuously. Route + authorization: `routes-logs-rest.test.ts`, whose matrix pins BOTH halves of the widened scope - that a room-mate IS reachable (a self-only implementation would fail it) and that an agent in an inaccessible room is NOT. A KILLED agent answers to a different rule (its own boss, or an office owner - the room lookup runs over the live roster and cannot serve a dead agent), so that half is pinned both ways too: the spawning boss and its agents get in, a room-mate of the corpse does not, and the refusal is byte-identical to the one an id that never existed gets. The guard itself is unit-pinned in `guards.test.ts` (`logSearchAccess`) over fakes, and the killed-list lookup in `guard-deps.test.ts`. ROUTE CAPS: `routes-table.test.ts` (`agents.logs` / `log:read`). Curl-card labels: `ui/log-view/isomux-curl.test.ts`.
- **Agent-built apps** - the registry (names, ports, data directories) and the supervisor (systemd units) are separate modules with separate tests, because only one of them touches the machine. `server/app-registry.test.ts` runs against a temp state root: allocation, delete freeing both the name and the port for reuse (with the legacy `app-history.json` proven unread - the malformed-bytes case is the one that shows nothing parses it), the data directory moved to `data/.retired/<name>-<deletedAt>` so a later app of the same name cannot inherit it, the fail-closed corruption posture (a registry that cannot read its file refuses every operation, reads included, rather than answering from a partial view), and the write ORDER that makes a half-finished delete recoverable - including the window where the archive landed and the record write did not, where the retry has to converge. `server/app-supervisor.test.ts` runs against a fake host - the one interface holding every systemctl, journalctl and unit-file operation - and pins the generated unit and launcher, the install/teardown call sequences, the two failures registration has to tell apart (systemd refusing the unit, versus the unit installing and the app's own process dying), the `systemctl show` parsing, and the state cache. `server/test-support/routes-apps-rest.test.ts` drives both through real HTTP with real minted tokens: the auth matrix, ownership taken from the token rather than the body, and the ordering between the two collaborators - a delete that cannot stop the app must NOT remove its record, or a live process is left holding a port under a name the registry has forgotten and could hand to whoever registers next. The harness injects the FAKE supervisor by default, and there is no way to ask it for the real one: systemd is machine-global, so a test that reached it would write unit files into whatever box the suite runs on and could stop that box's own apps. Real systemd is covered exactly once, gated, in `server/app-supervisor.live.test.ts` (`test:systemd` above) - it is the only thing that can catch a unit-file setting systemd rejects, and it earned its place immediately by catching one: `WorkingDirectory=` is not quote-parsed, so a quoted path passes every golden-file test and is refused by systemd as "path is not absolute".
- **App hostnames** - the transport that puts a registered app on its own origin, tested in layers because each layer is fail-closed on its own. Grammar and matching: `server/test-support/app-domain.test.ts`, `app-hosts.test.ts`. Dispatch, which is the regression net for every office route because the Host check runs ahead of URL parsing and auth: `app-host-dispatch.test.ts`. The relays, end to end against real scratch upstreams: `app-host-relay.test.ts` (handshake, byte-exact bodies, cookie strip, a stopped app refused without connecting) and `app-ws-upstream.test.ts` + `app-ws-relay.test.ts` + `app-ws-lifecycle.test.ts` (frame codec, bounded send queue, refusals before the 101, permits released exactly once). Certificate admission: `tls-ask.test.ts`. The app's own view of its address: `server/app-url-reconcile.test.ts`, `app-url-boot.test.ts`, `ui/components/AppsView.test.ts`. Installer and updater sides by source pin: `deploy/install-sh.test.ts`, `scripts/update-sh.test.ts`. Fill-loop tests here are byte-budgeted and THROW if a queue never refuses, so a mutation that removes a ceiling fails in milliseconds instead of taking the box down with it.
- **UI** - store-reducer invariants, the `apiFetch` harness, office grid, and room selection. Tests: `ui/store.test.ts`, `ui/api.test.ts`, `ui/office/grid.test.ts`, `ui/roomSelection.test.ts`, `ui/user-merge.test.ts`, `ui/agent-face.test.ts` (the tab-title state faces, frozen verbatim as user-facing copy), `shared/model-label.test.ts` (whether a model's display name already names its engine, which decides if the LogView header still shows the engine badge - pinned against `familyDisplayLabel` so the two cannot drift), `ui/storage-prune-form.test.ts` (the storage panel's destructive path, extracted to a pure seam precisely so it is testable: what request each action produces, that a DELETE is derived from the previewed PLAN rather than re-read from the form, that `keepPerAgent` is stated for transcripts and omitted for attachments, and that a plan no longer matching the form is rejected - the stale-preview race a screenshot cannot exercise).

Most server harnesses live under `server/test-support/`; the in-process server harness (`harness.test.ts`) boots the server against temp state with multiple authenticated users and sockets. Interactive terminal coverage is PARTIAL: the event-registry audience for `terminal_output`/`terminal_exit` is pinned (`event-registry.test.ts`), and the `terminal_open` buffered-replay ACL is now characterized (`projection.test.ts`: a restricted member receives zero `terminal_output` for a hidden agent, the requester gets exactly one, and a second visible user is not re-seeded; the buffer is seeded through the manager's test-only stubbed-terminal seam since FakeBackend has no PTY; task `39ce6225` closed). The broader interactive PTY path (live input/resize/close routing) stays deferred to a future stubbed-PTY/opt-in seam. The Codex subprocess lifecycle is adapter-contract plus opt-in live, not part of the main net.

## Infrastructure

What the T1 tier rests on (all built):

1. **Configurable state root + cleanup guard.** A single config module for the state root (an `ISOMUX_HOME`-style override) defaults to `~/.isomux` so production behavior is byte-for-byte unchanged; tests redirect to a temp dir. The cleanup guard refuses destructive cleanup unless the target realpath-resolves under the OS temp dir, and always refuses the real `~/.isomux`. (`server/config.test.ts`, `temp-state.test.ts`.)
2. **Constructor DI + FakeBackend.** `AgentManager`/`CronjobManager` are instantiable units whose collaborators are injected at construction (`ManagerDeps`: backend resolver, event sink, `officeState`; `CronjobManagerDeps`: backend resolver, event sink, env/user resolution, run persistence, clock/scheduler seam), with a production factory wiring the defaults. `FakeBackend` is injected through the resolver.
3. **In-process server harness.** Boots the server against a temp state root with multiple authenticated users and sockets (`harness.test.ts`).
4. **Script split.** `test` (no LLM) versus `test:live` (gated).

## No-loopback-bypass convention

There is no loopback bypass anywhere: every HTTP caller needs an identity (cookie or token). Tests pin the end state.

- The retired legacy prefixes (`/tasks*`, `/cronjobs*`, `/backup/status`) fail closed: anonymous 401 at the cookie wall, authenticated JSON 404, and no `Access-Control-Allow-Origin` or OPTIONS preflight on any of them (`routes-legacy-retired.test.ts`, which replaced the two suites that used to freeze their behavior).
- The deleted legacy affordance paths (`POST /agents/:id/{diff,edit-file,read-file,terminal-command}` and the cron-run read-file/diff) fail closed the same way, and the unified `POST /api/agents/:id/messages` route derives the sender from the token/cookie so a token-authed agent cannot spoof another agent's identity (`routes-affordances.test.ts`, `queue.test.ts`).

New affordance/message tests must keep asserting that identity is token- or cookie-derived, never body-derived. A new route must never be added to a loopback allowlist; there is none to add it to.

## Coverage references

Living "must-stay-covered" lists. When a change touches one of these areas, keep its coverage green.

### Projection / ACL checklist

The highest-value net for both the projection/ACL service and stable room IDs:

- Two users with overlapping but non-identical access connect simultaneously; `full_state` rooms are filtered per recipient, and agents carry a stable `roomId` identical across recipients.
- A hidden-room agent emits `log_entry`/`slash_commands`/session list; the restricted user never receives it.
- Moving an agent visible-to-hidden, hidden-to-visible, and visible-to-visible triggers the right refresh/replay and no stale transcript loss.
- Room close/reorder with restricted users: visible agents stay correct, presence is rebroadcast, logs/slash commands replay where expected. Reorder is per-user; a close is a bare `room_closed` delta (no dense remap, no full_state-on-close).
- Owner with hidden rooms: the main view respects access, while the owner-only `all_rooms_list` stays unfiltered.
- `create_room`: the creator has access, owners have access by rule (no fan-out), other members do not until granted.
- An owner editing a member's `allowedRooms` pushes a projected `full_state` to that user's existing sockets and clamps presence if access was revoked.
- Killed-agent summaries are filtered by `lastRoomId` before the cap; revive requires access to both the target room and `lastRoomId`.
- `list_sessions`/load logs for hidden agents do not leak ids/topics/timestamps.
- `presence_update` from a restricted user sends a global `currentRoomId`; the server validates it against the user's access and stores it, and `presence_list` emits the same id to every recipient, filtered to each recipient's visible rooms.
- Agent-to-agent `POST /api/agents/:id/messages` identity lookup does not become a room/ACL leak when one agent is in a hidden room.

### Persistence and migration

- Round-trip: write then load agents/users/tasks/cronjobs; assert shape and field preservation.
- The load-time migrations (e.g. `modelFamily`/`agentType` backfill, per-room `envFile` strip, the boot owner-grants access migration).
- The stable-room-IDs flatten: a pre-migration persisted shape (agents nested under rooms, `agent.room` derived on load) migrates to the flattened shape where each agent carries an explicit stable `roomId`. Migration bugs are likely and expensive.

### API contract

The full API spec (the Server API Spec section of the design doc) is the contract: every resource, its method, its required capability, its request/response shape, and the audience of any event it emits. Tests are TDD'd against the spec, and the typed route table enforces it in code (validation plus types). Covered by `routes-table.test.ts` plus the per-resource `routes-*-rest.test.ts` family.

## Traceability matrix

Feature → risk tier → deterministic test path → live/manual coverage. A coverage map, not a mandate: every feature in the canonical inventory (`docs/features.md`) should have a behavioral test of its critical mechanism at the lowest deterministic tier.

| Feature | tier | deterministic test path | live/manual |
|---|---|---|---|
| Onboarding / fresh install (welcome agents spawn across not-installed / not-logged-in / logged-in) | T1 (+opt-in T3) | fresh-install harness: temp ISOMUX_HOME, welcome-agent seed, FakeBackend per backend state | T3 live logged-in happy path |
| Multi-provider (mix Claude + Codex) | T2/T3 | adapter contract fixtures per backend | T3 smoke per provider |
| Subscription auth (works if CLI works) | T3 | env-detect unit; login-instructions unit | T3 manual login |
| Agents message each other (queue) | T1 | FakeBackend: POST message enqueues; coalescing order | - |
| Shared task board | T0/T1 | route + OfficeState task CRUD; attribution from token; per-recipient delta push (`server/events/task-delta.test.ts` for the pure rule, `routes-tasks-rest.test.ts` for the wire, `ui/store.test.ts` for the reducer arms) | - |
| Hierarchical system prompts | T0 | prompt assembly office/room/agent layering | - |
| Custom commands | T1 | slash_commands surfaced to client | - |
| Collaborate in conversations (multi-user) | T1 | multi-socket mixed human/agent queue | - |
| Live presence (ghosts) | T1 | presence harness: connectionId map, recipient indices | - |
| Invite-link access | T1 | `auth-invites.test.ts` (TTL ladder, `replacePriorForUsername` blast radius, non-consuming peek, mint/accept refusal matrices, concurrent accept, bootstrap sweep) + `auth-wall.test.ts` (the two-step flow over HTTP) | - |
| Session lifetime + revocation | T1 | `auth-sessions.test.ts` (both expiry clocks, refresh clamp, orphan sweep, notify-then-close on revoke/logout/evict, lockout arithmetic, prefix-collision refusal, lockout-check ordering) + `auth-wall.test.ts` (expired/revoked cookies at the HTTP and WS gates) | - |
| sessions.json userId migration | T1 | `auth-session-migration.test.ts` over a real cold reload: case-insensitive resolve, orphan eviction, fail-closed undated rows, write-back sticks | - |
| Self-hosted reachable / real-time | T1 | full_state on connect + delta sync | - |
| Mobile UI | manual | - | visual |
| Visual office / animated characters / skeuomorphic / themes | manual | - | visual |
| Auto-generated topic | T1/T3 | endpoint returns non-empty, at most 8 words (mechanism) | T3 quality spot-check |
| Terminal | T1 (partial) | event-registry audience for terminal_output/terminal_exit; terminal_open buffered-replay ACL covered (projection.test.ts, task 39ce6225); interactive PTY input/resize/close routing deferred to a future seam | opt-in real PTY |
| Editor | T1 | editor_open/save/external-change routes + path safety; watch deletion/recreation lifecycle + per-path revision guard (file-editor-watch.test.ts, routes-editor-rest.test.ts) | - |
| Diff tool | T0/T1 | diff summary unit + POST diff route | - |
| Browser preview card (preview-url) | T0/T1 | `preview-capture.test.ts`: capturePreview seam with a fake shell-script "browser" - strict validation matrix (syntax-only: any host incl. public is accepted since task fb02f521), no_browser/unreachable/capture_failed/timeout/busy codes, group-kill on deadline, temp-dir cleanup + slot release on every path. REST (`routes-agent-affordances-rest.test.ts`): full path via `ISOMUX_PREVIEW_BROWSER` fake → file-view log_entry with sanitized caption; 400s; cross-agent 403. ROUTE CAPS: `routes-table.test.ts`. The browser probe's absolute-path fallbacks (a systemd unit's PATH misses `/snap/bin`) are pinned through the `no_browser` message. | real-Chrome capture (needs Chrome on the box; exercised in normal office use) |
| Voice-to-text / TTS | manual | - | browser manual |
| Cron jobs | T1 | CronjobManager + FakeBackend: schedule/fire/run transcript/manual-run | - |
| Agent idle eviction | T1 | `agent-idle-eviction.di.test.ts`: demote guards, same-session wake, truthful wake wording + wake-notice delivery (e06b7e23), boot lazy-restore, demote-vs-message races | - |
| Image/PDF attachments | T0/T1 | buildUserMessage attachment inlining + upload route | - |
| Conversation branching | T1 | fork-chain assembly + usage accounting | - |
| Searchable conversation history (agent log search + tiered retrieval) | T0/T1 | `log-search.test.ts` (decode-before-match + the prefilter's soundness condition, fork asymmetry pinned against `loadLogWithAncestors`, tiers, `around` anchor retention, truncated-vs-timedOut, query bounds) + `log-search-isolation.test.ts` (SIGKILL bounds a NON-YIELDING scan: returns near the deadline, main-thread heartbeat keeps ticking, and the admission slot returns well under the cooperative budget - proving a real kill, not a give-up; concurrency caps and admission epochs) + `routes-logs-rest.test.ts` (three modes over HTTP; the auth matrix pinned in BOTH directions, live agents and killed ones) + `routes-table.test.ts` (`log:read`) | - |
| Notifications | T1 | turnHadHumanInput gating (mechanism) | - |
| Pre-tool-call safety hooks | T0/T1 | `safety-hooks.test.ts`: destructive commands and secret reads still denied; the Bash-side reader grammar both ways - `grep KEY .env` denied while `grep id_rsa ~/.ssh/config` (a pattern, not a path) is allowed, plus the per-command arity cases that separate them (`rg -r` takes a value, `grep -r` does not), quoted paths, readers behind a wrapper / `bash -c` / a substitution inside double quotes, and input redirection (`cat < .env` denied, the `cat <<< '.env'` here-string allowed); name-pattern kills (`pkill`/`killall`, and the `pgrep \| xargs kill` laundering) denied while PID- and port-targeted kills go through; `NotebookEdit`/`NotebookRead` guarded via `notebook_path`; path extraction fails CLOSED when a guarded tool's input names no path | - |
| Plugin system (incl. mem0 memory) | T1 | plugin pre/post turn hooks; mem0 injected content reaches the prompt | - |
| isomux-memory (filesystem cross-agent memory) | T0/T1 | STORE/RENDER (`memory-store.test.ts`): raw line round-trip, sha256 `version`, exact-duplicate guard, op-log entries, version-guarded REPLACE (conflict + force), per-scope size caps + trim notice - caps are a render/auto-load concern, the REST READ is intentionally UNCAPPED - and `measureForPromptMulti`, which sizes each auto-loaded scope against its cap BEFORE the trim, feeding the session-start memory notice. REST contract (`routes-memory-rest.test.ts`): auth wall, the three verbs (READ/APPEND/REPLACE) across all scopes, server-stamped provenance, exact-dup 409 with matched text, version-mismatch 409, op-log, and the permissive-REPLACE pin (any authenticated agent CAN REPLACE office memory). PROMPT (`system-prompt.test.ts`): auto-load attributed layer + no-boss-path + the three-verb affordance. ROUTE CAPS (`routes-table.test.ts`): `memory.read`/`memory.append`/`memory.replace`. NOTE: authority is permissive on EVERY verb (Nil's product decision); restraint lives in the system-prompt affordance, recovery in the op-log. There are no per-surface curation routes - the settings UI curates through the same `/api/memory` READ/REPLACE verbs (`useMemoryEditor`). | Playwright curation smoke DEFERRED (no e2e infra in repo; behavior covered at T0/T1) - manual UI verification + a future opt-in browser smoke |
| Agent-built apps (registry + systemd supervisor + hostnames) | T0/T1 (+gated real systemd) | `app-registry.test.ts` (allocation, the label ledger and generation walk, delete frees the name and port, the data directory set aside on delete, ignored legacy `app-history.json`, fail-closed corruption, delete write-order, certificate admissions) + `app-supervisor.test.ts` (unit + launcher rendering, install/teardown sequences, show-parsing, state cache, all against a fake host) + `test-support/routes-apps-rest.test.ts` (auth matrix, token-derived ownership, registry-versus-supervisor ordering, recovery verbs, logs) + `routes-table.test.ts` (`app:read` / `app:write`) + the hostname transport: `test-support/app-domain.test.ts` + `app-hosts.test.ts` (grammar, normalization, label matching), `app-host-dispatch.test.ts` (office routes untouched, only a single-label child diverts), `app-host-relay.test.ts` (handshake, byte-exact relay, cookie strip, stopped app refused without connecting), `app-ws-upstream.test.ts` + `app-ws-relay.test.ts` + `app-ws-lifecycle.test.ts` (frame codec, bounded send queue, refusals before the 101, permits released exactly once), `tls-ask.test.ts` (certificate admission gate), `app-url-reconcile.test.ts` + `test-support/app-url-boot.test.ts` (ISOMUX_APP_URL present-iff, boot reconcile, restart at most once) + `ui/components/AppsView.test.ts` (`appHref`: the url arm and the port arm) | `bun run test:systemd`: one real install → serve on the injected port → SIGKILL → restart counted → teardown round trip |
| Storage retention (disk usage + manual prune) | T0/T1 | `storage-usage.test.ts` (fixture state root: category split, sum-to-total, symlinks not followed, memoized read) + `storage-prune.test.ts` (the survival matrix: active session, keep-newest, fork ancestor incl. chains/siblings/cycles, attachments referenced by a transcript or owed to a queued message, symlinked parents, age cutoff; `applyPrune` re-plans, aborts on an out-of-fence candidate, refuses mtime-moved paths) + `routes-storage-rest.test.ts` (owner-only per-agent detail, owner-only prune, dry-run default) + `storage-report.test.ts` (the `/isomux-storage` markdown: total = state root + out-of-root locations, "none" for an unconfigured location, owner-only paths/per-agent rows, stated truncation, table-cell escaping of user-controlled agent names) + `test-support/storage-command.test.ts` (the handler's projection choice: owner vs member vs no-user-record, and no measurement on the refusal path) | - |
