# Writes the office's seccomp profile to each office node, where kubelet reads
# Localhost profiles. Not privileged: root with no capabilities, one hostPath.
apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: isomux-seccomp-installer
  namespace: isomux-node-setup
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: isomux-seccomp-installer
  template:
    metadata:
      labels:
        app.kubernetes.io/name: isomux-seccomp-installer
    spec:
      serviceAccountName: isomux-seccomp-installer
      automountServiceAccountToken: false
      enableServiceLinks: false
      terminationGracePeriodSeconds: 5
      nodeSelector:
        kubernetes.io/arch: amd64
        kubernetes.io/os: linux
        isomux.com/office-node: "true"
      containers:
        - name: install
          # The office image: already on the node, and it has bash and coreutils.
          image: ghcr.io/nmamano/isomux
          command:
            - bash
            - -c
            - |
              set -euo pipefail
              name=isomux-chromium-v1.json
              cp "/profile/$name" "/host/.$name.tmp"
              chmod 0644 "/host/.$name.tmp"
              mv -f "/host/.$name.tmp" "/host/$name"
              exec sleep infinity
          securityContext:
            runAsUser: 0
            runAsGroup: 0
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: [ALL]
            seccompProfile:
              type: RuntimeDefault
          readinessProbe:
            exec:
              command:
                - bash
                - -c
                - cmp -s /profile/isomux-chromium-v1.json /host/isomux-chromium-v1.json
            periodSeconds: 10
          resources:
            requests:
              cpu: 10m
              memory: 16Mi
            limits:
              memory: 64Mi
          volumeMounts:
            - name: profile
              mountPath: /profile
              readOnly: true
            - name: host
              mountPath: /host
      volumes:
        - name: profile
          configMap:
            name: isomux-seccomp
        - name: host
          hostPath:
            path: /var/lib/kubelet/seccomp/isomux
            type: DirectoryOrCreate
